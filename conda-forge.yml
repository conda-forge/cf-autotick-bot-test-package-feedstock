bot:
  automerge: true
conda_forge_output_validation: true
provider:
  linux: None
  linux_aarch64: azure
  linux_ppc64le: None
  osx: None
  win: None
  
azure:
  settings_linux:
    pool:
      name: conda-forge-internal-aarch64
    workspace:
      clean: all
    strategy:
      maxParallel: 1
docker:
  run_args: -t --rm
